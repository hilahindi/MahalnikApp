<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.MahalApp"
        tools:targetApi="31"
        android:windowSoftInputMode="adjustResize">

        <!-- LoginActivity is the first screen the user sees when opening the app -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity is launched after successful login or sign-up -->
        <activity android:name=".activities.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustNothing" />

        <!-- SignupActivity is used to create a new account -->
        <activity android:name=".activities.SignupActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustNothing" />

        <!-- ProfileActivity shows and edits the user's personal info -->
        <activity android:name=".activities.ProfileActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustNothing" />

        <!-- EventsActivity shows upcoming or past events -->
        <activity android:name=".activities.EventsActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustNothing" />

        <!-- DocumentsActivity for downloads and uploads -->
        <activity android:name=".activities.FilesActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustNothing" />

        <!-- ContactActivity to reach support -->
        <activity android:name=".activities.ContactActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustNothing" />

     </application>

 </manifest>
